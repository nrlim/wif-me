import type { ReactElement } from "react";
import { DashboardLoadingState } from "@/components/shared/dashboard-loading-state";

export default function DashboardLoading(): ReactElement {
  return <DashboardLoadingState />;
}
